{
  "name": "helpdesk-ai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "agent": "bun run apps/api/src/main.ts"
  },
  "packageManager": "bun@1.2.4",
  "devDependencies": {
    "turbo": "^2"
  }
}
